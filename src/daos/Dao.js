class Dao {
  async save(newItem) {
    throw "save no implentado";
  }

  async getById(idBuscado) {
    throw "getById no implentado";
  }

  async find(findObj) {
    throw "find no implentado";
  }

  async getAll() {
    throw "getAll no implentado";
  }

  async deleteById(idBuscado) {
    throw "deleteById no implentado";
  }

  async deleteAll() {
    throw "deleteAll no implentado";
  }

  async updateById(idBuscado, itemActualizado) {
    throw "updateById no implentado";
  }

  async connect() {
    throw "connect no implentado";
  }

  async disconnect() {
    throw "disconnect no implentado";
  }
}

export default Dao;
